docker_app: docker-compose.yml

flocker_hub:
    endpoint: https://flockerhub.com
    tokenfile: /root/fh.token

volumes:
    - name: mysql-data
      snapshot: docker-app-example-snapshot
      volumeset: docker-app-example