docker_app: docker-compose.yml

flocker_hub:
    endpoint: https://dp.dev.voluminous.io
    tokenfile: /root/vhut.txt

volumes:
    - name: registry-mount 
      snapshot: docker-images-1 
      volumeset: flidocker-example-registry
